---
- name: BASIC TASKS
  hosts: *
  gather_facts: false

  tasks:
    - debug: msg="Host is {{ ansible_fqdn }}"
    - name: Test host connectivity
      ping:

    - name: Creating an empty file
      file:
        path: "/tmp/h1.txt"
        state: touch
